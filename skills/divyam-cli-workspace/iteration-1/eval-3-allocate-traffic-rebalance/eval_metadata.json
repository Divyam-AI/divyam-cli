{
  "eval_id": 3,
  "eval_name": "allocate-traffic-rebalance",
  "prompt": "allocate 20% traffic to the checkout-router selector. current allocation is control 10, selector_disabled 90",
  "assertions": [
    "New allocation sums to exactly 100",
    "checkout-router bucket is set to 20",
    "control stays at or above ~1-2%",
    "Shows before->after and asks for confirmation before applying",
    "Uses sa update --traffic-allocation-config to apply"
  ]
}