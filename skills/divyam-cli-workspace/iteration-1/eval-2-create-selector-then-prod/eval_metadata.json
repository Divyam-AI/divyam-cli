{
  "eval_id": 2,
  "eval_name": "create-selector-then-prod",
  "prompt": "create a selector called checkout-router using the default extractor over the last 30 days, then move it to prod",
  "assertions": [
    "selector create includes --name checkout-router and an extractor strategy (-x)",
    "Emits the Selector Training dashboard URL (training-dashboard) after creation",
    "For --to-prod, asks for explicit confirmation before running",
    "Does not silently promote to prod"
  ]
}